import ServicesClient from '@/components/fitter/ServicesClient'

export const metadata = { title: 'Services — Fitment Portal' }

export default function FitterServicesPage() {
  return <ServicesClient />
}
